import { environment } from "src/environments/environment";

export const ACCOUNT_REGISTER: string = environment.apiUrl + 'accounts/register';
export const ACCOUNT_VERIFY: string = environment.apiUrl + 'accounts/otp';
