export interface City {
    code?: string,
    name?: string
}

export interface SortOrder {
    name?: string,
    code?: string
}

export interface Label {
    code?: string,
    name?: string,
    en?:string,
    vn?:string
}